<UserControl x:Class="TrainingContent.App.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Home"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Foreground="#FF1F2933" />

        <Border Grid.Row="1" Height="1" Background="#FFE0E0E0" Margin="0,12,0,20" />

        <StackPanel Grid.Row="2">
            <TextBlock Text="Training Content Generator"
                       FontSize="15"
                       FontWeight="SemiBold"
                       Foreground="#FF333333" />
            <TextBlock Text="教材の作成・録画・レビュー・管理を行うデスクトップアプリケーションです。"
                       Margin="0,10,0,0"
                       Foreground="#FF555555"
                       TextWrapping="Wrap" />
            <TextBlock Text="D1 では画面の外枠のみを実装しています。"
                       Margin="0,6,0,0"
                       Foreground="#FF888888"
                       TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</UserControl>
