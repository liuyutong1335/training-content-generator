<UserControl x:Class="TrainingContent.App.Views.ReviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="手順確認"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Foreground="#FF1F2933" />

        <Border Grid.Row="1" Height="1" Background="#FFE0E0E0" Margin="0,12,0,20" />

        <StackPanel Grid.Row="2">
            <TextBlock Text="録画から生成された操作手順を確認・編集する画面です。"
                       FontSize="15"
                       FontWeight="SemiBold"
                       Foreground="#FF333333"
                       TextWrapping="Wrap" />
            <TextBlock Text="この機能は現在準備中です。"
                       Margin="0,12,0,0"
                       Foreground="#FF888888"
                       TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</UserControl>
