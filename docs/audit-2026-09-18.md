# 総合監査レポート — training-content-generator（2026-09-18）

- 作成: 担当 A（Liu Yutong）+ Claude Code（4 系統の並列監査エージェント）
- 対象: `main`（`86da905`）+ 未マージ PR #11（B: `feature/event-capture-quality`）+ 未マージ `origin/feature/WPF-integration`（D）
- 検証ベース: `dotnet build` 0 警告 0 エラー / `dotnet test` **42/42 合格**（Core 13 + Capture 3 + Video 26・2026-09-18 実機）
- 総括: **Blocker 0 / Major 10 / Minor 約 20**。契約の型定義（§6/§7/§8/§12/§16）・Event Type（§9）・パス規則（§18）・直列化（§21）・Pause 除外タイムライン（§5.2）・events.jsonl 終端行（§20）・EndMs クランプ（§14）は実装が契約に忠実。

---

## 1. 契約（phase0-contract.md FROZEN）vs main 実装

> main の `src/` は Core / Capture / Video / EventCapture の 4 プロジェクト。StepBuilder・Redaction・Storage/App は未マージブランチ側のため、それらの到達経路は「参考」として確認。

| # | 深刻度 | 指摘 | 場所 | 備考 |
|---|---|---|---|---|
| M-1 | Major | UIA 失敗時も `uiElement: null` でなく全フィールド null の空オブジェクトを出力し、`UiAutomationQuality` が payload に出ない → 下流が「要素なし」と「UIA 失敗」を区別できない（§10 は「null でよい」の許容規定のため厳密違反ではない） | `OperationCaptureSession.cs:499-510`, `UiAutomationService.cs:222-239` | B 領域 |
| M-2 | Major | シングル左クリックの `timestampMs` がダブルクリック判定待ち（250〜900ms・既定 500）の分だけ実操作より遅れる（§5.1 同一論理時間軸を実害で崩す） | `GlobalMouseHook.cs:114-146`, `OperationCaptureSession.cs:318-321` | **PR #11 が QPC 物理時刻化で修正済み — マージで解決** |
| M-3 | Major | `ProjectValidator.Validate` が `steps: null` / `sourceEventIds: null` で NRE（§6.3/§22/§30 は Normalize or Reject を要求。§30 の「null array」のみテストも実装も不在） | `ProjectValidator.cs:48,78` | Core 領域 |
| M-4 | Major | 既存 events.jsonl への追記時に seq を 1 から振り直す（§8.1 単調増加違反・`stopped` が最終行でなくなる）。**契約が「再録画時の events.jsonl 扱い」を規定していないことが根本原因** → §27 の手順で契約追記が要る | `EventTimelineWriter.cs:59` | PR #11 の NEW-1 が正常経路は修正・壊れた最終行の分岐は未達（下記 3 の MIN-1） |
| m-1 | Minor | 準備中（CaptureStarted 前）Pause → Resume で NRE / Pause 込み Duration（lib の status 遷移は未実機検証） | `ScreenRecorderRecordingEngine.cs:82-108,249-270` | A 領域・要到達性確認 |
| m-2 | Minor | `recording.started` の `timestampMs=0` がリテラルでなく「概ね 0」（Stopwatch が 1ms 進み得る）→ `Append(..., 0, ...)` にすべき | `OperationCaptureSession.cs:116-117` | B 領域 |
| m-3 | Minor | `RebaseClockToNow()` 経路で rebase 前enqueue の Event が原点以前の時刻のまま書かれ得る（推奨手順＝CaptureStarted 契約 `Start()` では通らない経路） | `MasterClock.cs:59-68` | 保険経路のみ |
| m-4 | Minor | 音声なし録画（§7 では正当）で Title/Ending（anullsrc）と本編（音声なし）の concat が壊れ得る（ffmpeg 実機未検証） | `FfmpegVideoRenderer.cs:61,73-117` | A 領域 |
| m-5 | Minor | `Project.Recording == null` で字幕 0 件の動画が「成功」として返る | `FfmpegVideoRenderer.cs:52-55`, `StepTimelineBuilder.cs:32` | A 領域 |
| m-6 | Minor | スクリーンショット連番がプロセス静的カウンタで、再起動後の再録画で既存 original を上書きし得る | `ScreenshotCapture.cs:19,48` | B 領域 |
| m-7 | Minor | コード内の契約参照コメントの節番号誤り（§20→開発計画書 §20、§11→§7 など）トレーサビリ低下 | `IVideoComposer.cs:13` ほか 5 箇所 | A 領域（自モジュール分） |
| 観察 1 | Minor 相当 | §14 の EndMs 補完規則（次 Step 開始まで／最終 +4s／クランプ）は FROZEN 契約に無い規則（実装・テストは自己無矛盾。契約明文化を推奨） | `StepTimelineBuilder.cs:37-52` | §27 対象 |
| 観察 2 | Minor 相当 | UTC 日時が契約例の `Z` でなく `+00:00` で直列化される（実質要件は充足） | `TrainingJson.cs:13-20` | |
| 観察 3 | 情報 | 失敗経路の `uiElement` 以外、payload 形は契約例と完全一致 | `EventTimelineWriter.cs:9-41` | |

## 2. ドキュメント vs 実態 → **修正済み（PR #15・`90c8f5e`）**

- ~~README 担当分担表だけ Video=A 未更新~~ / ~~テスト数 16/16・32/32 の記載（実態 42/42）~~ / ~~契約参照 §11 の誤り~~ / ~~「sln は変更していない」の失効~~ / ~~integration-notes §2 で未マージ C ブランチの実装を実装済みと記載~~ → すべて PR #15 で修正
- 残る Minor（対応 undecided）: README 構成図・計画書 §12 に `TrainingContent.EventCapture` なし / README の tests 記載に存在しない `Manual.Tests`・`Integration.Tests` / README「現在の状態」チェックボックスが Phase 0 で止まま / 計画書 §3 技術構成表に FFmpeg なし / TPN に `System.Drawing.Common`・xunit 系・`ScreenRecorderLib 7.0.1`（spike のみ）の言及なし / spike ツールの大半に README なし
- 整合確認済み: sln 登録（Video/Video.Tests/PreparationCancelCheck）・TPN の ScreenRecorderLib Copyright（LICENSE 原文一致）・FFmpeg ビルド識別子（実バイナリ実測一致）・「WPF ブランチ 69/69」（静的カウント一致）・§5-3 の events.jsonl 記述・Overlay 未実装の明記

## 3. PR #11（B: EventCapture）— 総合判定 **Ship with fixes**

> 検証: temp clone で `dotnet build` 0 警告 0 エラー・フック非設置の 31 ケース全合格（Session 系 5 は実機影響のため未実行＝44/44 は未検証）。疑義 2 件は一時プローブで実機再現。

| # | 深刻度 | 指摘 |
|---|---|---|
| MAJ-1 | Major（実証済み・本 PR 新規導入） | `MasterClock._pauseIntervals`（List）を worker / アプリスレッドが無同期で読み書き → Pause/Resume 中の `ToCanonicalMs` で `InvalidOperationException` → **Event が無音で欠落**（7ms で再現）。修正案: 同一ロック保護 or イミュータブル配列 + `Interlocked.Exchange` |
| MAJ-2 | Major | UIA 無応答で `Join(15000)` がタイムアウトした場合、Stop 復帰後に `ProcessMouse` が `recording.stopped` の後へ `mouse.click` を追記（§20 違反・integration-smoke が fail する）。修正案: Stop 完了時 `_writer = null` / 全パス共通の stopped ゲート |
| MIN-1 | Minor（実証済み） | クラッシュで最終行が壊れた events.jsonl への追記で seq が 1 から再開（重複）+ 壊れた行に連結されて 1 行が完全破損。修正案: 全行 max(seq) 採用 / 追記前の末尾改行正規化 |
| MIN-2 | Minor | KB-2 fail-closed の条件漏れ（UIA プロパティ取得例外が `GenericContainerOnly`・`IsPassword=false` に潰れる経路） |
| MIN-3 | Minor | 遅延閾値 500ms の誤爆（正当な非パスワード入力バーストが sensitive 扱い = over-redaction） |
| MIN-4 | Minor | `<=` 境界で Resume 直後 1ms 盲窓 / `_pauseBoundaryMs` 永続 + Rebase で以後の Event を数秒廃棄し得る |
| MIN-5 | Minor | 保留クリックのタイマー世代競合（main と同一の既存・稀） |
| MIN-6 | Minor | `IsRecording` が Start 実行中も true（Start/Stop 競合で壊れたファイルを残し得る。目安順守で回避可） |
| INFO | 参考 | shortcut 短絡が alt を見ていない（Ctrl+Alt+S→"Ctrl+S"）/ Alt+Tab は無記録になる（契約上は妥当）/ 単発クリックは timestamp=物理時刻・スクショは遅延後画面の不一致 / ファイル行順の timestamp 単調性は崩れる / PR 本文の新規テスト内訳 19 は実測 18 |

確認済み: クリック時刻の物理時刻化（単発/ダブル/右クリック全て QPC）・連続クリック 1 回目消失の解消・KB-1 判定順序・UA-1 の `_textSync` 短縮・`<=` 境界の意図どおりの動作・fail-closed 主要経路・IsRecording 状態遷移・Stop 手順と保留クリック・NEW-1 正常経路・integration-notes §5 と duty-a-progress §9 の整合（「Dispose 後ハンドル解放なし」の記述は MAJ-2 の状況では限定必要）・OpenSteps 帰属表記

## 4. D ブランチ（origin/feature/WPF-integration・未マージ）

> 監査コミット `411c4b6`。main 最新取込済み（merge-base = `86da905`）・A/B/C のモジュールコードは無変更・merge-tree は clean。

### A の確認ポイント (a)〜(e)
- **(a) CaptureStarted 契約 `session.Start()`**: ✅ `RecordingCoordinator.cs:105,193`（`Volatile.Write` での先生成も正しい）
- **(b) preparation Cancel**: Coordinator ロジックは ✅（Duration=0 → fault・project.json に書かない）だが **UI に Cancel が存在せず到達不能**（下記指摘 1）
- **(c) 0 バイト残留 MP4 の削除試行**: 未実装（ただし必ず失敗するため実害なし・Minor）
- **(d) Progress / キャンセルの UI 接続**: ❌ `options: null`・CancellationTokenSource 不在・ProgressBar/Cancel ボタンなし・生成中 nav lock なし
- **(e) MP4 ロック配慮**: 未考慮（latent・R-1 参照）

### 指摘
| # | 深刻度 | 指摘 |
|---|---|---|
| 1 | Major | preparation 中に Stop も Cancel も発行不能（`RecordingView.xaml.cs:151-152` の IsEnabled 条件・Cancel ボタン不在）。watchdog もエスケープハッチもなく、CaptureStarted が来ない場合は nav lock + close 拒否で **UI が完全に詰む**（タスクマネージャのみ） |
| 2 | Major | 動画生成の Progress/キャンセルが UI 未接続（`VideoGenerationCoordinator.cs:132`・`ContentsView.xaml.cs:306`）。Steps 実装後に即 Major 化 |
| 3 | Major（A と共同） | **再録画時の preparation cancel で既存の正常な `raw/recording.mp4` が 0 バイトで上書きされ、かつハンドルリークで削除不能**。project.json は旧録画のまま → 「録画あり」表示が偽になる。A の「削除試行して失敗なら無視」回答では整合性が回復しない |
| 4〜9 | Minor | Cancel 時の誤った失敗メッセージ / RecordingCoordinator・VideoGenerationCoordinator のテスト 0 本 / `.gitattributes` の全体ポリシー変更を feature ブランチで実施 / `Generate_Click` に catch なし（async void）/ 動画生成は現状 Steps.Count>0 のため到達不能（指摘 2 は Steps 実装時に同時修正で可） |

### 残留リスク（マージ時）
- **R-1（要実機再検証・最重要）**: `5d2b568`（Recorder 即解放の撤回）以降、**通常停止直後の MP4 ロック有無は未検証**（spike 判定 5 の「ロックなし」は撤回前計測の可能性）。マージ後、現行 engine で preparation-cancel-check 相当を再実行し `File.Delete` / ffmpeg 読み込みを確認
- **R-2**: A 側で「Record 先を temp にして CaptureStarted 後に canonical へ move」等の方式検討（A/D 共同）
- **R-3**: Cancel 実装時は `StopButton.IsEnabled` 条件緩和と watchdog 再議論
- **R-4**: Progress 接続時は `Progress<T>` を UI スレッドで生成・生成中 nav lock
- **R-5**: マージ元は **origin 側（`411c4b6`）**。ローカル `feature/WPF-integration` は 31 コミット遅れで RC-2 / dispose-timing 修正を含まない。**PR が未作成**（レビュー記録なし）
- **R-6**: fault 分類テスト最低 1 本を推奨

## 5. 対応分担（提案）

| 担当 | 項目 |
|---|---|
| **A（Liu）** | R-2 のエンジン方式検討（temp 録画 → CaptureStarted 後 move）・R-1 のロック再検証・m-1（準備中 Pause）の到達性確認・m-4/m-5（音声なし・Recording null）のガード・自モジュールの m-7 コメント修正 |
| **B** | PR #11 の MAJ-1（MasterClock 同期化）・MAJ-2（stopped 終端ゲート）・MIN-1（seq フォールバック）を修正してからマージ。m-1〜m-6 は follow-up |
| **C** | （C ブランチ監査は本レポート対象外。マージ後に integration-notes §2 の再確認） |
| **D** | Cancel ボタン追加（R-3）・Progress/キャンセルの UI 接続（R-4）・PR 作成（R-5）・Coordinator テスト（R-6） |
| **チーム（§27）** | 契約改訂: 再録画時の events.jsonl / screenshots/original 扱い（M-4・m-6）・§14 EndMs 補完規則の明文化・UTC 表記（`Z`）の統一可否 |

## 6. 未検証（正直な記載）

- ScreenRecorderLib の status 遷移（準備中 Pause 中に `RecorderStatus.Recording` が出るか）＝ m-1 の到達条件
- Pause 中に lib が MP4 へフレームを書かないこと（lib 内部）
- m-4（音声なし録画の concat）の ffmpeg 実行確認
- PR #11 の Session 系テスト 5 ケースと 44/44 の全数（実デスクトップ影響のため未実行）
- §5.1「0ms 一致」の frame 単位検証（duty-a-progress §3-12 の「未解決の注記」のまま）
- D ブランチ WPF の実ビルド / 実機ロック計測（R-1）
